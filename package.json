{
  "name": "realism-grade",
  "version": "0.1.0",
  "private": true,
  "description": "EUR/USD BUY entry alert system (V1): deterministic rules engine for Ahmad's Confluence Framework",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "validate:ticks": "tsx scripts/validate-ticks.ts",
    "backtest": "tsx scripts/backtest.ts",
    "backtest:2026": "tsx scripts/backtest.ts docs/data/Exness_EURUSD_2026_0?.zip --end 2026-09-25T00:00:00Z --out docs/backtest/2026-preliminary.md --json data/backtest/2026-preliminary.json --title 'Preliminary backtest: EUR/USD 2026-01-01 to 2026-09-24 (partial year)'"
  },
  "dependencies": {
    "yaml": "^2.9.1",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/node": "^22.20.4",
    "tsx": "^4.23.15",
    "typescript": "^5.9.3",
    "vitest": "^3.2.7"
  }
}
